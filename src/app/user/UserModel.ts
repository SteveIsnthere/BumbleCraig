export interface EssentialUserData {
  user_id: number;
  name: string;
  figure_id: number;
}

export let dummyEssentialUserData: EssentialUserData = {
  user_id: 0,
  name: "dummy",
  figure_id: 0
}
